name: Deploy to Cloud Run

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: 'projects/476256146919/locations/global/workloadIdentityPools/github-pool/providers/github-provider'
          service_account: 'github-actions@project-fa804700-a17e-4ac5-bfb.iam.gserviceaccount.com'

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build with Maven
        run: mvn package

      - name: Configure Docker for Artifact Registry
        run: gcloud auth configure-docker us-central1-docker.pkg.dev --quiet

      - name: Build and push Docker image
        run: |
          docker build -t us-central1-docker.pkg.dev/project-fa804700-a17e-4ac5-bfb/calculator/calculator:${{ github.sha }} .
          docker push us-central1-docker.pkg.dev/project-fa804700-a17e-4ac5-bfb/calculator/calculator:${{ github.sha }}

      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy calculator \
            --image us-central1-docker.pkg.dev/project-fa804700-a17e-4ac5-bfb/calculator/calculator:${{ github.sha }} \
            --platform managed \
            --region us-central1 \
            --allow-unauthenticated \
            --port 8080
